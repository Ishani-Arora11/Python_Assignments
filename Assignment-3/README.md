Assignment 3

Task 1
Takes a number as input and returns it's factorial using a recursive function named fact(num).
factorial of n = n * (n - 1) * (n - 2) * ---- * 2 * 1

Task 2
Takes a number as input and make computations on that number using sqrt, log and sin functions from math module in order to compute square root, logarithm and sine of the number.
